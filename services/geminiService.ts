import { CalendarEvent } from '../types';

export const parseNaturalLanguageToEvents = async (prompt: string): Promise<CalendarEvent[]> => {
  console.warn("AI capabilities have been disabled in this build.");
  return [];
};